{
  "name": "canvas-little-helper",
  "version": "1.0.0",
  "main": "dist/index.js",
  "license": "MIT",
  "private": true,
  "type": "module",
  "workspaces": [
    "lib/",
    "example/"
  ],
  "files": [
    "dist/*"
  ],
  "size-limit": [
    {
      "path": "lib/dist/index.js",
      "limit": "10 kB"
    }
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "npm run test --workspace canvas-little-helper",
    "lint": "eslint lib/ example/",
    "prepare": "husky install",
    "zip": "zip dist/index.zip dist/index.js -9dv",
    "size": "npm run build --workspace lib && size-limit"
  },
  "devDependencies": {
    "@size-limit/preset-small-lib": "^7.0.3",
    "eslint": "^8.3.0",
    "eslint-plugin-mocha": "^9.0.0",
    "husky": "^7.0.4",
    "size-limit": "^7.0.5"
  }
}
